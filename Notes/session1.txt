- what is node js
    - run-time environment enbles javascript to run on the server not just the browser, الخ APIs فيمكننا كتابة قواعد البيانات والـ
    - high performance => built on V8 engine (chrome engine) => so effecient with real-time applications (chats,..)

- حل مشكلة ايه؟
    - in the past you were writing the front end with js and the back with pyhton, phb,.. first problem
        second problem is too many requests make the server get slower and eventually breakdown cuz phb,... use the synchronous way.
    - node is built upon the non-blocking i/o system 
        => if encounters a blocking task i will tell Event viewer to handle it, i keep going and he calls me if finished


- node REPL 
    => read evaluate print loop => من التيرمينال node تشغيل

- browser vs node 
    => both are built in V8 engine, browser works for the client cares about the frontend,
    => node cares about the server, manage database, network, files,..

- Modules 
=> are functionalities set up in a file or more 
=> 1. Built-in (os, path,..)   2. User-defined (local)   3. npm (node package manager) 


