- Jsend => system that unified the shape of the returned data (ساعات بنرجع الداتا كاوبجكت ساعات غيرها فهو بيوحد ده)
        even if its just a simple response so all respones takes one shape
        => an object that has 2 keys one for the "status" other for the data/message

=> status: succsess => required keys: status / data  
           fail: invalid input/ customer error => required keys: status / data
           Error => srever/ database => required keys: status / message

        * data always an object
        * message always a string

    castError => type conversion faluire => فلما ميتحولش اذا المستخدم اخطا في كتابته او كتبه ناقص ObjectId مونجوز بيحول الاي دي السترينج اللي جايله ل  

- utilities =>  استخدمناه بشكل اكثر احترافية لما عملنا فولدر الـ jsend

- Pagination ارسال الداتا بشكل منفصل بدل من ارسال مليون دوكيومينيت مرة واحدة ارسالهم علي فترات
 => example
const query = req.query;
    const limit = query.limit || 2; // || means by default put 2
    const page = query.page || 1;
    const skip = (page - 1) * limit;
 
- dotenv 
- handling wrong URLs

    
- CORS: if we are on port 4000 and the front-end is on port 5000, he cant take data from server 
        الطريقة اللي بيسمح بيها السيرفر للمتصفح انه ياخد بيانات من السيرفر
        ممكن تسمح للجميع بالوصول او مواقع محددة

- async wrapper:بيبلغ عن الخطا فقط لكن مش بيحله  middleware بيرجع  
    - فبترجع برومس promise based cuz its async ليه دالتين جوه الورابر مش واحدة فقط لاخبار الورابر بالخطا وخلصنا, لان الدالة الاصلية 
    فلازم دالة وسيطة مترجمة تستقبل البرومس اللي راجع وتبلغ الـدالة العليا middleware واكسبريس مش بيفهم برومس هو بيفهم       

- regular middleware(1) vs error handler middleware (2)
    - express knows them by how many params do they contain, if 3 its regular if 4 its error handling (error, req, res, next)
        - لازم تخليهم اربعة عشان اكسبريس يبعت الخطا ليها 
