1. (npm init -y) to make the package then download express, nodemon
2. make the script in package cuz we will run the program from nodemon => npm run ...

- شوية ثوابت
1. 
app.use(express.json()); // Body parser 
                        // line says => read the body you get from client as json, Object بيمكنك يكون ليك اكسيس علي البيانات دي في شكل  
2.
const {body, validationResult} = require('express-validator');
- body => Validation المدخل بتاع اليوزر والبارام التاني هو النتيجة بتاع الـ

// Courses بنعمل كرود علي الـ 
    - http methods vs CRUD
    // GET => return  data => Read  
    // POST => create data => Create
    // PUT/PATCH => update data => PUT to fully update, patch to partially update => Update
    // DELETE => delete data => Delete   


- جميل لكن مش هيشتغل '/api/courses/1' لذلك احنا لو عملنا id الحاجه اللي بتميز كل كورس عن التاني هي الـ
بالشكل ده Api لازم نعمله 

// GET
app.get('/api/courses/1', (req, res)=>{ // Api 
    const course = courses.find((course)=> course.id === 1);
    res.json(course);
})
لكن ده مش عملي لاني لو عندي 1000 كورس هضطر اكرر العملية دي الف مرة
اللي عملناها Get الحل في دالة الـ 


------------------
- difference between res.json() and res.send()
   => سيند تقبل اي نوع بيانات عكس الجيسون اللي لا يقبل غير معرف ولا دالة
   جيسون ديما يحول البيانات لجيسون سترينج اما الاخر ياخد البيانات بحالتها
   جيسون بيحول اي بيانات الي سترينج لذلك لا يقبل الدوال علي سبيل المثال
------------------



// POST
=> postman => request body من خلاله نقدر نبعت داتا للباك ايند
اذا استدعي الميدلوير وهو جواه بودي بارسرBody parser مش هقدر اقرا البودي غير ب

// Express validator => check on body written by client and return with the result
 express validator => Express is minimalist تذكر ان  Built-in مجموعة من الميدلويرز اتعملت وليست 

----------------------------------------------------------------------------------
Router() => instance from routing object
     
     - when routing we can route every 
        // router.get('/', coursesControllers.getAllCourses);
        // router.get('/:id', coursesControllers.getCourse);
    - but if we have routes have the same path we can compine them in one router 
        // like in the routes.js file.
    
- شوف المشروع قبل وبعد التقسيم والشغل
    